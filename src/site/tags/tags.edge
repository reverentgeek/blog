---
pagination:
  data: collections.tags
  size: 1
  alias: tag
  addAllPagesToCollections: true
layout: 'layouts/tag.edge'
permalink: '{{ tag.url }}'
---

@if(tag.description)
{{{ tag.description }}}
@else
{{ tag.count.posts }} posts
@end
