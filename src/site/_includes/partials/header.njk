{% from "partials/icon.njk" import icon %}

{# {% if page.url != "/shop/" %}
  <div class="text-white px-6 py-4 border-0 relative mb-4 bg-rgblue text-center">
    <a class="inline-block align-middle mr-8" href="/shop/">
      Want me to draw something for you?
      <b>Click here!</b>
    </a>
  </div>
{% endif %} #}

<header class="px-6 py-2  bg-rgblue/75 flex flex-wrap items-center lg:px-16">
  {% include "partials/navigation.njk" %}
</header>
{% if page.url == "/" and heroImage %}
  <div class="mb-4 w-full border-t-6 border-rgblue/15">
    <picture>
      {% if heroImage.srcset.avif %}
        <source type="image/avif" srcset="{{ heroImage.srcset.avif }}" sizes="{{ heroImage.sizes }}">
      {% endif %}
      {% if heroImage.srcset.webp %}
        <source type="image/webp" srcset="{{ heroImage.srcset.webp }}" sizes="{{ heroImage.sizes }}">
      {% endif %}
      <img
        class="w-full"
        eleventy:ignore
        loading="eager"
        fetchpriority="high"
        decoding="async"
        src="{{ heroImage.fallback.url }}"
        {% if heroImage.srcset.fallback %}srcset="{{ heroImage.srcset.fallback }}"{% endif %}
        sizes="{{ heroImage.sizes }}"
        width="{{ heroImage.fallback.width }}"
        height="{{ heroImage.fallback.height }}"
        alt="{{ heroImage.alt }}">
    </picture>
  </div>
{% endif %}
