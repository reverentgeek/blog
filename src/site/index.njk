---
pagination:
  data: collections.posts
  size: 10
  alias: posts
  reverse: true
---

{% extends 'layouts/default.njk' %}
{% from "partials/card.njk" import card %}

{% set title = "Home" %}

{% block content %}
	{# <section class="">
		<div>Hi, I'm David! I write software, draw illustrations, play guitar, make videos, and speak at conferences all over the world!</div>
		<button>About Me</button>
		<button>Contact Me</button>
	</section> #}
    <section class="post-list">
        {% for post in posts %}
			{{ card(post) }}
        {% endfor %}
    </section>

    {% include "partials/pagination.njk" %}
{% endblock%}
